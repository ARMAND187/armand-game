const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY); async function run() { const { data } = await supabase.from('profiles').select('id').eq('username', 'armand1').single(); await supabase.rpc('equip_shop_item', { p_item_id: '3c95d42d-3cfd-42da-9b3d-370d61e408c1' }); const { data: p2 } = await supabase.from('profiles').select('equipped_flair').eq('username', 'armand1'); console.log(p2); } run();  
