const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data: { user } } = await supabase.auth.signUp({ email: 'test_rls@example.com', password: 'password123' }); const { error } = await supabase.from('profiles').update({ equipped_flair: 'test' }).eq('id', user.id); console.log(error); } run();  
